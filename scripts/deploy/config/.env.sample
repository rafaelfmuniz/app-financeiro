# Prod environment example
NODE_ENV=production
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=finance_user
DB_PASS=change_me
DB_NAME=finance_db

PORT=3000
JWT_SECRET=change_me_long_random
JWT_REFRESH_SECRET=change_me_refresh_long_random
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=30m
DEFAULT_TENANT_NAME=RM

MASTER_EMAIL=master@controle.com
MASTER_PASSWORD=change_me
MASTER_NAME=Master
MASTER_USERNAME=master
ADMIN_EMAIL=admin@socialblueti.com
ADMIN_PASSWORD=admin123

# SMTP (envio de e-mails)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_REPLY_TO=

# Base URL (usado para links de redefinição de senha)
APP_BASE_URL=http://localhost:3000
